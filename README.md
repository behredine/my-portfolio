# Behredin Eshetu Portfolio

Premium freelance software engineering portfolio for Behredin Eshetu.

## Run locally

Use the included static server from this directory:

```bash
npm run dev
```

Then open `http://localhost:5173`.

The site uses React and Framer Motion through ESM CDN imports in `app.js`, so it does not require a build step.
